# Galdr plugin marketplace

This directory is generated by the protected plugin publishing workflow.

- `index.json` is the schema-1 index consumed by Galdr.
- `metadata.json` contains presentation metadata for the marketplace website.
- Plugin archives are immutable assets on version-specific GitHub Releases.

Do not edit generated JSON files directly in `galdr-hub`.
